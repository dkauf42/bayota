#!/bin/bash

BINPATH=`dirname $0`

echo

# start script with --daemon as an argument to restart itself
# detached from the current shell.
case "$1" in
    -d|--daemon)
        $0 < /dev/null &> /dev/null & disown
        exit 0
        ;;
    *)
        ;;
esac

"$BINPATH/bayota_efficiency.py" $@