--- # study spec start
baseyear: 2010

# geography dictionary
geography:
  scale: county
  entity: "Adams, PA"

# objective dictionary
objective:
  sense: minimize
  expression: Total_Cost
  indices: None

# constraints list
constraints:
  - bound: lower
    expression: Percent_Reduction
    indices: PLTNTS
  - bound: upper
    expression: Available_Acres
    indices: LRSEGS, LOADSRCS

--- # document end